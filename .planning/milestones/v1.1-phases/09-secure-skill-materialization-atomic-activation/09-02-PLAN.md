---
phase: 09-secure-skill-materialization-atomic-activation
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/skills/install.rs
autonomous: true
requirements:
  - SKILL-02
user_setup: []
must_haves:
  truths:
    - Only the managed allowlist is staged into an installed skill.
    - Unsafe payload content is rejected before activation.
  artifacts:
    - install staging enforces a managed-file allowlist plus explicit rejections for symlinks, hidden content, VCS directories, and build artifacts.
    - install lifecycle tests cover unsafe payload rejection.
  key_links:
    - Phase 10 can reuse this managed payload policy for scan and serve enforcement instead of inventing a second trust boundary.
---

<objective>
Narrow the installed skill payload to the managed allowlist.

Purpose: the current pipeline copies whole source trees and can activate unsafe
content. Phase 9 must make the installed skill root contain only validated
managed assets and managed metadata.
Output: explicit allowlist staging plus rejection tests for unsafe trees.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-secure-skill-materialization-atomic-activation/09-CONTEXT.md
@.planning/phases/09-secure-skill-materialization-atomic-activation/09-RESEARCH.md
@.planning/phases/09-secure-skill-materialization-atomic-activation/09-VALIDATION.md
@.planning/phases/09-secure-skill-materialization-atomic-activation/09-01-PLAN.md
@src/skills/install.rs
@src/skills/models.rs
@src/skills/parser.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage only managed assets and managed metadata</name>
  <files>src/skills/install.rs</files>
  <read_first>src/skills/install.rs, src/skills/models.rs, src/skills/parser.rs, .planning/research/ARCHITECTURE.md</read_first>
  <action>Replace the recursive whole-tree copy path with managed payload staging driven by the canonical skill asset model. Copy only `SKILL.md`, managed `scripts/**`, managed `references/**`, and explicit Sumac-managed metadata written by the install pipeline. Do not let arbitrary extra files ride along into the installed skill root.</action>
  <verify>rg -n "assets|SKILL.md|references|scripts|metadata" src/skills/install.rs</verify>
  <acceptance_criteria>
    - the install path no longer recursively copies the whole source tree blindly
    - the staged payload is derived from the managed asset inventory
    - `.sxmc-source.json` remains the managed provenance artifact written by Sumac
  </acceptance_criteria>
  <done>The installed payload is product-defined and limited to managed assets.</done>
</task>

<task type="auto">
  <name>Task 2: Reject unsafe payload contents before activation</name>
  <files>src/skills/install.rs</files>
  <read_first>src/skills/install.rs, .planning/codebase/CONCERNS.md, .planning/phases/09-secure-skill-materialization-atomic-activation/09-VALIDATION.md</read_first>
  <action>Add validation and tests that reject symlinks plus hidden, VCS, and build-artifact content before activation. Cover both install and update paths where practical, and surface explicit lifecycle failures instead of silently ignoring unsafe content.</action>
  <verify>cargo test --quiet install_skill</verify>
  <acceptance_criteria>
    - unsafe payload contents trigger install failure before activation
    - staged installs do not silently copy hidden or VCS content
    - lifecycle tests prove the rejection behavior
  </acceptance_criteria>
  <done>Unsafe payload contents are stopped at staging time rather than reaching active skills.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] install lifecycle tests covering unsafe payload rejection
- [ ] `cargo test --quiet`
- [ ] `cargo clippy --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Only managed assets activate
- Unsafe payload contents are rejected before install or update activation
  </success_criteria>

<output>
After completion, create `.planning/phases/09-secure-skill-materialization-atomic-activation/09-02-SUMMARY.md`
</output>

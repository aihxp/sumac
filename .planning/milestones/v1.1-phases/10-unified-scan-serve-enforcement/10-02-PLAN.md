---
phase: 10-unified-scan-serve-enforcement
plan: 02
type: execute
wave: 1
depends_on:
  - 10.01
files_modified:
  - src/server/handler.rs
autonomous: true
requirements:
  - SEC-02
user_setup: []
must_haves:
  truths:
    - Skill file listings and direct file reads expose only the allowlisted managed asset set.
    - Unmanaged files under a skill root are rejected even when they physically exist on disk.
  artifacts:
    - handler file listing derives from `Skill.assets` instead of recursive filesystem traversal.
    - direct file resolution denies unmanaged relative paths before reading them.
  key_links:
    - Plan 10-01 establishes the same canonical managed asset inventory for scanner coverage.
---

<objective>
Tighten MCP file serving to the managed skill asset allowlist.

Purpose: the current handler treats "inside the skill directory" as good enough,
which lets unmanaged files remain listable and readable after install.
Output: managed-only file listing and direct file access that match the scanned
and installed skill inventory.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-unified-scan-serve-enforcement/10-CONTEXT.md
@.planning/phases/10-unified-scan-serve-enforcement/10-RESEARCH.md
@.planning/phases/10-unified-scan-serve-enforcement/10-VALIDATION.md
@src/server/handler.rs
@src/skills/models.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serve only managed asset paths</name>
  <files>src/server/handler.rs</files>
  <read_first>src/server/handler.rs, src/skills/models.rs, .planning/research/ARCHITECTURE.md</read_first>
  <action>Refactor the handler's skill file helpers so listings and direct file resolution derive from the canonical managed asset inventory instead of recursive filesystem traversal plus base-dir containment checks alone. Preserve the current MCP tool names and response shapes.</action>
  <verify>rg -n "skill.assets|list_skill_files|resolve_skill_file_path" src/server/handler.rs</verify>
  <acceptance_criteria>
    - `get_skill_details` reports only managed skill files
    - `get_skill_related_file` accepts allowlisted managed paths and rejects unmanaged ones
    - the handler no longer recurses arbitrary files under the skill base directory
  </acceptance_criteria>
  <done>MCP file exposure is narrowed to the managed asset allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Add regressions for allowlisted serving and unmanaged-file denial</name>
  <files>src/server/handler.rs</files>
  <read_first>src/server/handler.rs, .planning/codebase/CONCERNS.md</read_first>
  <action>Add focused tests that prove managed nested assets remain listable and readable while unmanaged files inside the same skill directory are not exposed through handler file APIs.</action>
  <verify>cargo test --quiet handler</verify>
  <acceptance_criteria>
    - managed nested assets remain visible in handler listings
    - unmanaged files are rejected even when present inside the skill directory
    - existing escape-path rejection still passes
  </acceptance_criteria>
  <done>Serve enforcement is covered by explicit handler regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] focused handler tests for managed-only listings and unmanaged-file denial
- [ ] existing path-escape rejection still passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- MCP file listings show only managed assets
- Direct file reads reject unmanaged in-tree files
  </success_criteria>

<output>
After completion, create `.planning/phases/10-unified-scan-serve-enforcement/10-02-SUMMARY.md`
</output>

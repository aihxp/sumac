---
phase: 08-watch-runtime-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/app/watch.rs
  - tests/cli_integration.rs
autonomous: true
requirements:
  - WATCH-03
user_setup: []
must_haves:
  truths:
    - Slow notify commands and webhook deliveries are bounded and do not stall the main watch loop indefinitely.
    - Notification failures are isolated and explicitly reported without breaking stable watch command behavior.
  artifacts:
    - `src/app/watch.rs` uses async timeout-aware helpers for notify commands and webhooks.
    - Watch integration tests cover at least one slow-side-effect scenario.
  key_links:
    - Phase 11 can preserve the watch command contract because Phase 8 already proves the new seam behaves like the old surface under failure.
---

<objective>
Harden watch side effects so long-running sessions remain responsive.

Purpose: the extracted watch seam still needs explicit timeout and isolation
behavior for notify commands and webhooks. Right now those effects can stall
frame progression and make watch appear hung.
Output: timeout-bounded, non-blocking side-effect helpers plus regression tests
for slow or failing notification paths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-watch-runtime-hardening/08-CONTEXT.md
@.planning/phases/08-watch-runtime-hardening/08-RESEARCH.md
@.planning/phases/08-watch-runtime-hardening/08-VALIDATION.md
@.planning/phases/08-watch-runtime-hardening/08-01-PLAN.md
@src/app/watch.rs
@tests/cli_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeout-bounded async notification helpers</name>
  <files>src/app/watch.rs</files>
  <read_first>src/app/watch.rs, .planning/research/STACK.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Refactor watch notification handling so file appends stay lightweight, notify commands run through async subprocess handling, and webhook posts use explicit timeout-aware HTTP requests. Ensure failures are surfaced as isolated event errors rather than aborting or blocking the main watch loop. Keep existing payload shapes and watch output intact.</action>
  <verify>rg -n "timeout|tokio::process|notify command|webhook" src/app/watch.rs</verify>
  <acceptance_criteria>
    - `src/app/watch.rs` contains timeout-aware notification helpers
    - blocking `std::thread::sleep` and blocking command execution are not part of the main watch loop
    - notification failures do not propagate as fatal loop errors by default
  </acceptance_criteria>
  <done>Watch notification side effects are bounded and isolated from the main loop.</done>
</task>

<task type="auto">
  <name>Task 2: Add slow-side-effect regression tests</name>
  <files>tests/cli_integration.rs</files>
  <read_first>tests/cli_integration.rs, src/app/watch.rs, .planning/phases/08-watch-runtime-hardening/08-VALIDATION.md</read_first>
  <action>Add or extend watch integration tests to prove responsiveness under slow notification conditions. Cover at least one slow webhook or hanging notify command path and assert the watch flow still renders or exits under bounded behavior instead of hanging indefinitely. Preserve the current healthy and unhealthy payload assertions.</action>
  <verify>cargo test --quiet watch_</verify>
  <acceptance_criteria>
    - `tests/cli_integration.rs` contains a slow-side-effect watch regression
    - the new regression would fail under the old blocking implementation
    - existing watch integration tests still pass
  </acceptance_criteria>
  <done>Responsiveness under slow notifications is proved by regression tests, not only by reasoning.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet watch_`
- [ ] `cargo test --quiet`
- [ ] `cargo clippy --all-targets --all-features -- -D warnings`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `sxmc watch` remains responsive under bounded side-effect failures
- Existing output, exit semantics, and notification payload shapes remain stable
  </success_criteria>

<output>
After completion, create `.planning/phases/08-watch-runtime-hardening/08-02-SUMMARY.md`
</output>

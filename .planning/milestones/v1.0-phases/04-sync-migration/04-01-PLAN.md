# Plan 04-01: Migrate `sync` onto the Shared Core/App Seam and Prove Parity

## Goal

Give `sync` a dedicated migrated write-side service and prove parity between the
default/core route and the legacy rollback route.

## Tasks

1. Add a focused app/service module for `sync`.
2. Move `SyncRequest` and sync-specific execution there.
3. Keep the generic golden-path seam delegating to the dedicated sync slice.
4. Add direct parity checks for core/default and legacy routing.

## Done When

- `sync` is no longer implemented as generic code in
  `src/app/golden_path.rs`
- direct parity proof exists for sync routing
- validation remains green

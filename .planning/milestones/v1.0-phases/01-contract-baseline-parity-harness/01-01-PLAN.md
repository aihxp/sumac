---
phase: 01-contract-baseline-parity-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/GOLDEN_PATH_CONTRACT.md
  - docs/PRODUCT_CONTRACT.md
  - docs/USAGE.md
  - docs/README.md
autonomous: true
requirements:
  - PAR-01
user_setup: []
must_haves:
  truths:
    - Maintainers have one rewrite-focused contract inventory for `setup`, `add`, `status`, and `sync`.
    - The inventory captures command shape, install-scope behavior, JSON fields, generated artifacts, and exit/stdout expectations without changing the public CLI.
  artifacts:
    - docs/GOLDEN_PATH_CONTRACT.md exists with sections for `setup`, `add`, `status`, and `sync`.
    - Maintained docs link to the golden-path contract inventory.
  key_links:
    - PRODUCT_CONTRACT and USAGE point maintainers to the new golden-path inventory as the rewrite source of truth.
---

<objective>
Publish the golden-path interface inventory that later rewrite phases can use as
their stable contract baseline.

Purpose: Phase 1 needs one compact, versionable contract artifact before any
core/app migration starts.
Output: A dedicated golden-path contract doc plus maintained doc links that make
it the clear rewrite reference for `setup`, `add`, `status`, and `sync`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-baseline-parity-harness/01-CONTEXT.md
@.planning/phases/01-contract-baseline-parity-harness/01-RESEARCH.md
@docs/PRODUCT_CONTRACT.md
@docs/STABILITY.md
@docs/USAGE.md
@docs/README.md
@src/cli_args.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the dedicated golden-path contract inventory</name>
  <files>docs/GOLDEN_PATH_CONTRACT.md</files>
  <read_first>docs/PRODUCT_CONTRACT.md, docs/STABILITY.md, docs/USAGE.md, src/cli_args.rs, src/main.rs</read_first>
  <action>Create `docs/GOLDEN_PATH_CONTRACT.md` as the rewrite source of truth for `sxmc setup`, `sxmc add`, `sxmc status`, and `sxmc sync`. Include concrete sections for each command covering: primary entrypoints and naming aliases, install-scope controls (`--root`, `--global`, `--local` where relevant), stable machine-readable top-level fields, generated file/state side effects, and stdout/stderr plus exit-behavior expectations. Keep the scope explicitly limited to the golden path and note that the product surface remains broader than this rewrite baseline.</action>
  <verify>rg -n "^## (setup|add|status|sync)$|--global|install_scope|generated" docs/GOLDEN_PATH_CONTRACT.md</verify>
  <acceptance_criteria>
    - `docs/GOLDEN_PATH_CONTRACT.md` contains `## setup`, `## add`, `## status`, and `## sync`
    - `docs/GOLDEN_PATH_CONTRACT.md` contains `--global`
    - `docs/GOLDEN_PATH_CONTRACT.md` contains `install_scope`
  </acceptance_criteria>
  <done>The new inventory doc exists and inventories the four golden-path commands concretely.</done>
</task>

<task type="auto">
  <name>Task 2: Link maintained docs to the new rewrite baseline</name>
  <files>docs/PRODUCT_CONTRACT.md, docs/USAGE.md, docs/README.md</files>
  <read_first>docs/GOLDEN_PATH_CONTRACT.md, docs/PRODUCT_CONTRACT.md, docs/USAGE.md, docs/README.md</read_first>
  <action>Update maintained docs so the new inventory is discoverable without replacing existing product-wide docs. Add a short note in `docs/PRODUCT_CONTRACT.md` that Phase 1's rewrite baseline lives in `docs/GOLDEN_PATH_CONTRACT.md`. Add a rewrite-baseline pointer in `docs/USAGE.md` near the `setup -> add -> status -> sync` lifecycle. Add an entry in `docs/README.md` so maintainers can find the file from the docs index.</action>
  <verify>rg -n "GOLDEN_PATH_CONTRACT" docs/PRODUCT_CONTRACT.md docs/USAGE.md docs/README.md</verify>
  <acceptance_criteria>
    - `docs/PRODUCT_CONTRACT.md` contains `GOLDEN_PATH_CONTRACT.md`
    - `docs/USAGE.md` contains `GOLDEN_PATH_CONTRACT.md`
    - `docs/README.md` contains `GOLDEN_PATH_CONTRACT.md`
  </acceptance_criteria>
  <done>The maintained docs all point to the golden-path contract inventory.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "GOLDEN_PATH_CONTRACT" docs/PRODUCT_CONTRACT.md docs/USAGE.md docs/README.md` returns matches
- [ ] `rg -n "^## (setup|add|status|sync)$" docs/GOLDEN_PATH_CONTRACT.md` returns four sections
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The rewrite baseline can be found from the maintained docs without guessing
- The published inventory is specific enough for later migration phases to cite directly
  </success_criteria>

<output>
After completion, create `.planning/phases/01-contract-baseline-parity-harness/01-01-SUMMARY.md`
</output>

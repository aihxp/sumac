---
phase: 01-contract-baseline-parity-harness
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tests/cli_integration.rs
  - scripts/test-sxmc.sh
  - docs/VALIDATION.md
  - docs/COMPATIBILITY_MATRIX.md
autonomous: true
requirements:
  - PAR-02
  - ROL-04
user_setup: []
must_haves:
  truths:
    - Golden-path parity is enforced by executable checks before migrated logic becomes the default path.
    - Rewrite validation reporting explains how maintainers track parity, reliability, and cross-platform behavior for the migration target.
  artifacts:
    - tests/cli_integration.rs contains rewrite-focused golden-path parity checks.
    - scripts/test-sxmc.sh contains a golden-path rewrite parity section.
    - docs/VALIDATION.md describes the rewrite parity harness and where to read it.
  key_links:
    - Validation docs point to the golden-path contract inventory and to the tests that enforce it.
---

<objective>
Build characterization, artifact, and regression tracking that proves the
golden-path rewrite baseline is executable rather than just documented.

Purpose: later migration phases need objective parity gates before cutover.
Output: targeted rewrite parity checks in Rust and shell validation plus
maintained docs that explain the rewrite-specific reporting path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-baseline-parity-harness/01-CONTEXT.md
@.planning/phases/01-contract-baseline-parity-harness/01-RESEARCH.md
@.planning/phases/01-contract-baseline-parity-harness/01-VALIDATION.md
@.planning/phases/01-contract-baseline-parity-harness/01-01-SUMMARY.md
@docs/GOLDEN_PATH_CONTRACT.md
@docs/VALIDATION.md
@docs/COMPATIBILITY_MATRIX.md
@tests/cli_integration.rs
@scripts/test-sxmc.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit rewrite parity checks to Rust integration coverage</name>
  <files>tests/cli_integration.rs</files>
  <read_first>tests/cli_integration.rs, docs/GOLDEN_PATH_CONTRACT.md, docs/PRODUCT_CONTRACT.md</read_first>
  <action>Add rewrite-focused integration tests that assert the maintained golden-path contracts for `setup`, `add`, `status`, and `sync`. Reuse the existing tempdir and command helpers already in `tests/cli_integration.rs`. Cover concrete stable fields and behaviors already promised: `install_scope`, stable command identifiers (`command: add` / `command: setup`), `ai_knowledge` + `recovery_plan` in `status`, and `changed_count` / `entries` in `sync`. Keep the tests narrow and additive so they characterize current behavior rather than redesign it.</action>
  <verify>cargo test --quiet test_rewrite_golden_path_</verify>
  <acceptance_criteria>
    - `tests/cli_integration.rs` contains `test_rewrite_golden_path_`
    - `tests/cli_integration.rs` contains `ai_knowledge`
    - `tests/cli_integration.rs` contains `changed_count`
  </acceptance_criteria>
  <done>Rust integration coverage names and checks the rewrite golden path explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell-level rewrite regression tracking and document it</name>
  <files>scripts/test-sxmc.sh, docs/VALIDATION.md, docs/COMPATIBILITY_MATRIX.md</files>
  <read_first>scripts/test-sxmc.sh, docs/VALIDATION.md, docs/COMPATIBILITY_MATRIX.md, docs/GOLDEN_PATH_CONTRACT.md</read_first>
  <action>Add a clearly labeled rewrite/golden-path parity section to `scripts/test-sxmc.sh` that exercises the maintained onboarding loop and reports pass/fail for `setup`, `add`, `status`, and `sync` behaviors already covered by the contract inventory. Then update `docs/VALIDATION.md` to explain that the rewrite baseline is enforced by the dedicated golden-path contract doc plus targeted Rust/shell checks. Update `docs/COMPATIBILITY_MATRIX.md` with a small note or row that the rewrite target has explicit parity coverage across the maintained onboarding lifecycle.</action>
  <verify>rg -n "golden-path|rewrite parity|setup|add|status|sync" scripts/test-sxmc.sh docs/VALIDATION.md docs/COMPATIBILITY_MATRIX.md</verify>
  <acceptance_criteria>
    - `scripts/test-sxmc.sh` contains `golden-path` or `rewrite parity`
    - `docs/VALIDATION.md` contains `GOLDEN_PATH_CONTRACT.md`
    - `docs/VALIDATION.md` contains `rewrite`
  </acceptance_criteria>
  <done>Shell validation and maintained docs both describe and enforce rewrite parity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet test_rewrite_golden_path_`
- [ ] `bash scripts/test-sxmc.sh --json /tmp/sxmc-phase01-results.json`
- [ ] `rg -n "GOLDEN_PATH_CONTRACT|rewrite parity|golden-path" docs/VALIDATION.md docs/COMPATIBILITY_MATRIX.md scripts/test-sxmc.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Golden-path rewrite parity has both Rust and shell-level proof
- Maintainers can see where rewrite parity is tracked in the normal validation story
  </success_criteria>

<output>
After completion, create `.planning/phases/01-contract-baseline-parity-harness/01-02-SUMMARY.md`
</output>

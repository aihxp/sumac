# Plan 05-01: Migrate `add` onto the Shared Core/App Seam and Prove Parity

## Goal

Give `add` a dedicated onboarding slice and promote shared onboarding logic into
its own reusable service for later `setup` consolidation.

## Tasks

1. Add a shared onboarding service module.
2. Add an `add`-specific service module.
3. Move `AddRequest` and add execution into that dedicated slice.
4. Add direct parity checks for core/default and legacy routing.

## Done When

- `add` no longer lives as generic code in `src/app/golden_path.rs`
- shared onboarding logic exists in a reusable module
- parity proof exists for add routing

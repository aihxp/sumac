---
phase: 09-secure-skill-materialization-atomic-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skills/install.rs
autonomous: true
requirements:
  - SKILL-01
  - SKILL-03
user_setup: []
must_haves:
  truths:
    - Local, git repo root, and git repo subpath installs resolve the intended skill directory.
    - Install and update activate atomically so failures do not leave partial live state.
  artifacts:
    - `materialize_source_dir()` no longer resolves from `temp.keep()`.
    - install/update activation stages and swaps validated content without deleting the live target first.
  key_links:
    - Plan 09-02 can narrow the trust boundary on top of a correct, atomic lifecycle base.
---

<objective>
Fix source materialization and atomic activation in the skills lifecycle.

Purpose: the current install path resolves git sources from the wrong
directory, leaks clone temps, and couples staging to unsafe replacement.
Output: correct local/git materialization plus staged activation that preserves
the old install on failure.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-secure-skill-materialization-atomic-activation/09-CONTEXT.md
@.planning/phases/09-secure-skill-materialization-atomic-activation/09-RESEARCH.md
@.planning/phases/09-secure-skill-materialization-atomic-activation/09-VALIDATION.md
@src/skills/install.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct source materialization for local and git-backed installs</name>
  <files>src/skills/install.rs</files>
  <read_first>src/skills/install.rs, .planning/codebase/CONCERNS.md, .planning/research/STACK.md</read_first>
  <action>Refactor source materialization so local paths still validate directly, but git-backed sources clone into scoped temporary storage, resolve repo-root and repo-subpath inputs from the actual clone directory, and do not persist transient clone directories via `TempDir::keep()`. Preserve the existing outward install/update entrypoints and metadata format.</action>
  <verify>rg -n "materialize_source_dir|tempdir|clone|repo_subpath" src/skills/install.rs</verify>
  <acceptance_criteria>
    - `src/skills/install.rs` no longer uses `temp.keep()` for transient clone materialization
    - git repo root and repo subpath resolution are handled from the clone directory, not the temp parent
    - local path installs still validate through `ensure_skill_dir`
  </acceptance_criteria>
  <done>Source materialization resolves the correct skill directory without leaking transient clone paths.</done>
</task>

<task type="auto">
  <name>Task 2: Make activation preserve the previous install on failure</name>
  <files>src/skills/install.rs</files>
  <read_first>src/skills/install.rs, .planning/research/ARCHITECTURE.md</read_first>
  <action>Adjust the staged install/update activation path so the replacement skill is fully built before the live target is removed or swapped. Preserve atomic replacement behavior and make failed activation leave the previous target intact instead of partially replaced or missing.</action>
  <verify>rg -n "install_skill_into_target|rename|remove_dir_all|staging" src/skills/install.rs</verify>
  <acceptance_criteria>
    - staging occurs before the active target is touched
    - failed activation preserves the previous target contents
    - install and update still report the same stable outward result shape
  </acceptance_criteria>
  <done>The lifecycle path is atomic and failure-safe for installs and updates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] focused install tests for local, git root, and git subpath sources
- [ ] regression proving failed update leaves prior installed content intact
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Git and local materialization are correct
- Failed activation does not destroy or partially replace the active install
  </success_criteria>

<output>
After completion, create `.planning/phases/09-secure-skill-materialization-atomic-activation/09-01-SUMMARY.md`
</output>

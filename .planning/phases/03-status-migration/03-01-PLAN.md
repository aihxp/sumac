# Plan 03-01: Migrate `status` onto the Shared Core/App Seam and Prove Parity

## Goal

Give `status` a dedicated read-only service boundary inside `src/app/` and
prove parity between the default/core route and the legacy rollback route.

## Tasks

1. Add a focused app/service module for `status`.
2. Move `StatusRequest` and status-specific execution there.
3. Keep the generic golden-path seam delegating to the dedicated status slice.
4. Add parity tests comparing core/default and legacy routing.

## Done When

- `status` is no longer implemented as a generic method blob inside
  `src/app/golden_path.rs`
- direct parity proof exists for status routing
- full validation remains green

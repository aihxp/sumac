---
phase: 10-unified-scan-serve-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/security/skill_scanner.rs
autonomous: true
requirements:
  - SEC-01
user_setup: []
must_haves:
  truths:
    - Managed script and reference assets are scanned from the canonical `Skill.assets` inventory.
    - Unreadable and non-UTF-8 managed files create explicit findings instead of being silently skipped.
  artifacts:
    - `scan_skill()` no longer relies on the legacy compatibility views as its primary managed-file coverage path.
    - scanner regressions prove explicit findings for unreadable and invalid UTF-8 managed assets.
  key_links:
    - Plan 10-02 can reuse the same managed asset policy to tighten MCP file serving.
---

<objective>
Make scanner coverage fail closed over canonical managed skill assets.

Purpose: the current scanner silently skips unreadable files and only walks the
legacy compatibility views, which leaves a mismatch between installed assets
and verified assets.
Output: canonical managed-asset scanning with explicit findings for read and
decode failures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-unified-scan-serve-enforcement/10-CONTEXT.md
@.planning/phases/10-unified-scan-serve-enforcement/10-RESEARCH.md
@.planning/phases/10-unified-scan-serve-enforcement/10-VALIDATION.md
@src/security/skill_scanner.rs
@src/skills/models.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan managed assets from the canonical inventory</name>
  <files>src/security/skill_scanner.rs</files>
  <read_first>src/security/skill_scanner.rs, src/skills/models.rs, .planning/research/ARCHITECTURE.md</read_first>
  <action>Refactor skill scanning so managed file coverage is derived from `Skill.assets`, with body and frontmatter checks preserved. Limit file-content scanning to managed asset kinds that may later be served or executed, and keep outward report behavior stable except for the new explicit failure findings.</action>
  <verify>rg -n "SkillAssetKind|scan_.*asset|SL-IO|from_utf8|std::fs::read" src/security/skill_scanner.rs</verify>
  <acceptance_criteria>
    - scanner coverage derives managed files from `Skill.assets`
    - managed scripts and references remain scanned for dangerous content
    - the legacy compatibility views are no longer the sole source of coverage
  </acceptance_criteria>
  <done>Managed scan coverage matches the canonical installed asset inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Surface explicit findings for read and decode failures</name>
  <files>src/security/skill_scanner.rs</files>
  <read_first>src/security/skill_scanner.rs, .planning/research/STACK.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Replace silent file-read skips with explicit findings for unreadable managed files and invalid UTF-8 managed files. Add focused regressions that prove those findings are emitted for managed script/reference assets.</action>
  <verify>cargo test --quiet skill_scanner</verify>
  <acceptance_criteria>
    - unreadable managed assets produce explicit findings
    - invalid UTF-8 managed assets produce explicit findings
    - clean text assets still scan without false positive IO findings
  </acceptance_criteria>
  <done>Scanner failures are visible instead of silently bypassing coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] focused scanner regressions for unreadable and invalid UTF-8 managed assets
- [ ] managed asset scanning still catches dangerous or injected content
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Managed assets are scanned from the canonical inventory
- Read and decode failures surface as explicit findings
  </success_criteria>

<output>
After completion, create `.planning/phases/10-unified-scan-serve-enforcement/10-01-SUMMARY.md`
</output>

---
phase: 07-canonical-asset-inventory-watch-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/server/mod.rs
autonomous: true
requirements:
  - WATCH-02
user_setup: []
must_haves:
  truths:
    - Polling and native watch behavior are anchored to the same managed asset surface instead of separate traversal rules.
    - Nested managed asset changes are observable to the polling watch path.
  artifacts:
    - `src/server/mod.rs` fingerprints canonical managed assets rather than only top-level script/reference entries.
    - watch regression tests prove nested script and nested reference changes alter the fingerprint.
  key_links:
    - Phase 8 can harden the watch runtime on top of a correctness-proofed asset model instead of a shallow hash workaround.
---

<objective>
Use the canonical managed asset inventory from Plan 07-01 to make watch
correctness parity real for nested managed assets.

Purpose: the current polling fingerprint only hashes one directory level, so it
can silently miss nested changes even though those files are part of the
effective skill surface.
Output: polling fingerprint logic and regression tests that treat nested
managed assets as first-class watch inputs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-canonical-asset-inventory-watch-parity/07-CONTEXT.md
@.planning/phases/07-canonical-asset-inventory-watch-parity/07-RESEARCH.md
@.planning/phases/07-canonical-asset-inventory-watch-parity/07-VALIDATION.md
@.planning/phases/07-canonical-asset-inventory-watch-parity/07-01-SUMMARY.md
@src/skills/parser.rs
@src/server/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fingerprint the canonical managed asset inventory</name>
  <files>src/server/mod.rs</files>
  <read_first>src/server/mod.rs, src/skills/parser.rs, .planning/phases/07-canonical-asset-inventory-watch-parity/07-RESEARCH.md, .planning/phases/07-canonical-asset-inventory-watch-parity/07-01-SUMMARY.md</read_first>
  <action>Refactor `compute_skill_fingerprint` in `src/server/mod.rs` so it fingerprints the canonical managed asset inventory produced by `parse_skill` instead of manually hashing only top-level `scripts/` and `references/` directory entries. Remove the shallow per-directory special case for nested managed assets and replace it with a helper that hashes each managed asset path plus relevant metadata in stable sorted order. Keep discovery snapshots and manifest hashing behavior intact, and preserve current user-facing logging/output behavior for watch reloads.</action>
  <verify>rg -n "compute_skill_fingerprint|parse_skill|assets|managed asset" src/server/mod.rs</verify>
  <acceptance_criteria>
    - `src/server/mod.rs` still contains `fn compute_skill_fingerprint`
    - `src/server/mod.rs` contains `parse_skill`
    - `src/server/mod.rs` no longer relies only on `hash_directory_files(&dir.join(\"scripts\")` and `hash_directory_files(&dir.join(\"references\")` as the nested managed-asset source of truth
  </acceptance_criteria>
  <done>The polling fingerprint path is driven by the canonical managed asset inventory instead of shallow directory hashing.</done>
</task>

<task type="auto">
  <name>Task 2: Add nested-asset polling parity regression tests</name>
  <files>src/server/mod.rs</files>
  <read_first>src/server/mod.rs, src/skills/parser.rs, .planning/phases/07-canonical-asset-inventory-watch-parity/07-VALIDATION.md</read_first>
  <action>Add regression tests to `src/server/mod.rs` that prove nested managed asset changes alter the watch fingerprint. Create one test named `test_compute_skill_fingerprint_changes_when_nested_script_changes` and one named `test_compute_skill_fingerprint_changes_when_nested_reference_changes`. Each test should create a temporary skill fixture with nested files under `scripts/**` or `references/**`, compute the fingerprint, mutate the nested file content, compute the fingerprint again, and assert the fingerprint changed. Keep the existing skill-body fingerprint test green.</action>
  <verify>cargo test --quiet test_compute_skill_fingerprint_changes_when_skill_body_changes test_compute_skill_fingerprint_changes_when_nested_script_changes test_compute_skill_fingerprint_changes_when_nested_reference_changes</verify>
  <acceptance_criteria>
    - `src/server/mod.rs` contains `test_compute_skill_fingerprint_changes_when_nested_script_changes`
    - `src/server/mod.rs` contains `test_compute_skill_fingerprint_changes_when_nested_reference_changes`
    - `cargo test --quiet test_compute_skill_fingerprint_changes_when_skill_body_changes test_compute_skill_fingerprint_changes_when_nested_script_changes test_compute_skill_fingerprint_changes_when_nested_reference_changes` exits `0`
  </acceptance_criteria>
  <done>Polling parity is backed by regression tests for nested managed assets rather than only a top-level skill-body check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet test_compute_skill_fingerprint_changes_when_skill_body_changes test_compute_skill_fingerprint_changes_when_nested_script_changes test_compute_skill_fingerprint_changes_when_nested_reference_changes`
- [ ] `rg -n "nested_script_changes|nested_reference_changes" src/server/mod.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Polling watch correctness includes nested managed assets
- Phase 7 leaves Phase 8 with a proven asset model instead of a shallow-hash fallback
  </success_criteria>

<output>
After completion, create `.planning/phases/07-canonical-asset-inventory-watch-parity/07-02-SUMMARY.md`
</output>

---
phase: 07-canonical-asset-inventory-watch-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skills/models.rs
  - src/skills/parser.rs
autonomous: true
requirements:
  - WATCH-01
user_setup: []
must_haves:
  truths:
    - Sumac has one canonical managed asset inventory for each parsed skill instead of separate shallow parser-only views.
    - Nested managed files under `scripts/` and `references/` are represented in the parsed skill model.
  artifacts:
    - `src/skills/models.rs` defines canonical managed asset types on `Skill`.
    - `src/skills/parser.rs` recursively collects managed assets and keeps compatibility views for scripts and references.
    - parser tests prove nested managed assets are included.
  key_links:
    - The parser's asset inventory becomes the source of truth that later watch parity work can consume directly.
---

<objective>
Create the canonical managed skill asset model that Phase 7 needs before any
watch parity fixes can be trusted.

Purpose: the current parser only indexes top-level `scripts/` and
`references/` files, which already diverges from the recursive file surface the
server can expose and the nested files users can edit.
Output: a recursive managed asset inventory on `Skill`, plus parser coverage
that proves nested managed files are part of the authoritative model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-canonical-asset-inventory-watch-parity/07-CONTEXT.md
@.planning/phases/07-canonical-asset-inventory-watch-parity/07-RESEARCH.md
@.planning/phases/07-canonical-asset-inventory-watch-parity/07-VALIDATION.md
@src/skills/models.rs
@src/skills/parser.rs
@src/server/handler.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical managed asset types to the skill model</name>
  <files>src/skills/models.rs</files>
  <read_first>src/skills/models.rs, .planning/phases/07-canonical-asset-inventory-watch-parity/07-CONTEXT.md, .planning/phases/07-canonical-asset-inventory-watch-parity/07-RESEARCH.md</read_first>
  <action>Extend `src/skills/models.rs` with a canonical managed asset representation. Add a `SkillAssetKind` enum with concrete variants for `SkillFile`, `Script`, and `Reference`. Add a `SkillAsset` struct that records at least the relative path, absolute path, and kind for each managed asset. Add an `assets: Vec<SkillAsset>` field to `Skill`. Keep the existing `scripts` and `references` fields intact so current callers continue compiling while the new inventory becomes the authoritative model.</action>
  <verify>rg -n "enum SkillAssetKind|struct SkillAsset|assets: Vec<SkillAsset>" src/skills/models.rs</verify>
  <acceptance_criteria>
    - `src/skills/models.rs` contains `enum SkillAssetKind`
    - `src/skills/models.rs` contains `struct SkillAsset`
    - `src/skills/models.rs` contains `assets: Vec<SkillAsset>`
  </acceptance_criteria>
  <done>The skill model exposes one canonical managed asset inventory without removing the compatibility fields existing callers use today.</done>
</task>

<task type="auto">
  <name>Task 2: Parse recursive managed assets and prove nested coverage</name>
  <files>src/skills/parser.rs</files>
  <read_first>src/skills/parser.rs, src/skills/models.rs, src/server/handler.rs, .planning/phases/07-canonical-asset-inventory-watch-parity/07-RESEARCH.md</read_first>
  <action>Refactor `src/skills/parser.rs` so `parse_skill` builds the new canonical asset inventory recursively. Keep `SKILL.md` represented as a managed asset and recursively discover files under `scripts/` and `references/` using stable sorted ordering. Derive the existing `scripts` and `references` vectors from the canonical inventory so top-level compatibility remains intact. Add parser tests that create nested files such as `scripts/nested/check.sh` and `references/guides/guide.md`, then assert the canonical inventory contains those relative paths and that the compatibility lists still include the expected direct resource/script entries.</action>
  <verify>cargo test --quiet parser::tests::test_parse_skill parser::tests::test_parse_skill_recurses_nested_assets</verify>
  <acceptance_criteria>
    - `src/skills/parser.rs` contains `test_parse_skill_recurses_nested_assets`
    - `src/skills/parser.rs` contains `scripts/nested/check.sh`
    - `src/skills/parser.rs` contains `references/guides/guide.md`
    - `cargo test --quiet parser::tests::test_parse_skill parser::tests::test_parse_skill_recurses_nested_assets` exits `0`
  </acceptance_criteria>
  <done>The parser emits a recursive managed asset inventory and tests prove nested managed files are modeled explicitly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "enum SkillAssetKind|struct SkillAsset|assets: Vec<SkillAsset>" src/skills/models.rs` returns matches
- [ ] `cargo test --quiet parser::tests::test_parse_skill parser::tests::test_parse_skill_recurses_nested_assets`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The parsed skill model has one authoritative managed asset inventory
- Nested managed assets are covered by tests before watch logic starts consuming the model
  </success_criteria>

<output>
After completion, create `.planning/phases/07-canonical-asset-inventory-watch-parity/07-01-SUMMARY.md`
</output>

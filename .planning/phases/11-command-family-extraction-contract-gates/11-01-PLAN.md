---
phase: 11-command-family-extraction-contract-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/mod.rs
  - src/app/skills.rs
  - src/main.rs
autonomous: true
requirements:
  - CORE-06
  - CORE-07
user_setup: []
must_haves:
  truths:
    - `skills` runs through a dedicated app-service seam rather than a large inline `main.rs` branch.
    - The extracted service preserves existing list, info, run, create, install, and update behavior.
  artifacts:
    - new `src/app/skills.rs` request/service layer exists
    - `Commands::Skills` in `src/main.rs` becomes thin request assembly plus service delegation
  key_links:
    - Plan 11-02 can extract the remaining CLI-facing `serve` wrapper and add contract gates on top of the new seams
---

<objective>
Extract the `skills` command family behind a focused app-service seam.

Purpose: `src/main.rs` still owns a large inline `skills` branch even though
the underlying behavior is already stable and testable.
Output: typed `skills` requests and a dedicated service that reuses existing
helpers while preserving current CLI behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-command-family-extraction-contract-gates/11-CONTEXT.md
@.planning/phases/11-command-family-extraction-contract-gates/11-RESEARCH.md
@.planning/phases/11-command-family-extraction-contract-gates/11-VALIDATION.md
@src/main.rs
@src/app/mod.rs
@src/command_handlers.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a `skills` app service and typed requests</name>
  <files>src/app/mod.rs, src/app/skills.rs</files>
  <read_first>src/app/mod.rs, src/app/watch.rs, src/command_handlers.rs, src/skills/install.rs</read_first>
  <action>Create `app::skills` with request types for the current `skills` actions and a service that orchestrates the existing command handlers, generator, and install/update pipeline without changing outward behavior.</action>
  <verify>rg -n "struct .*Skills|enum .*Skills|SkillsService" src/app/skills.rs src/app/mod.rs</verify>
  <acceptance_criteria>
    - `app::skills` exposes a focused request/service seam
    - the new service reuses existing lower-level behavior rather than re-implementing it
    - outward `skills` behavior remains stable
  </acceptance_criteria>
  <done>`skills` orchestration has a dedicated app-service seam.</done>
</task>

<task type="auto">
  <name>Task 2: Thin `main.rs` skills dispatch down to request assembly</name>
  <files>src/main.rs</files>
  <read_first>src/main.rs, src/app/skills.rs</read_first>
  <action>Replace the large inline `Commands::Skills` match arm with thin request assembly and delegation to `app::skills::SkillsService`, preserving existing parsing, printing, and exit behavior.</action>
  <verify>rg -n "Commands::Skills|SkillsService" src/main.rs</verify>
  <acceptance_criteria>
    - `Commands::Skills` no longer owns the full orchestration inline
    - `main.rs` remains responsible for CLI parsing and high-level composition only
    - compile-time ownership between `main.rs` and `app::skills` is clear
  </acceptance_criteria>
  <done>`main.rs` is thinner and the `skills` family is extracted cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] focused tests for migrated `skills` list/info/run and install/update flows
- [ ] `main.rs` no longer contains the large inline `skills` orchestration block
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `skills` command-family orchestration moves behind `app::skills`
- `main.rs` only assembles requests and delegates
  </success_criteria>

<output>
After completion, create `.planning/phases/11-command-family-extraction-contract-gates/11-01-SUMMARY.md`
</output>

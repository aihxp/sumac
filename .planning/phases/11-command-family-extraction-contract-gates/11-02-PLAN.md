---
phase: 11-command-family-extraction-contract-gates
plan: 02
type: execute
wave: 1
depends_on:
  - 11.01
files_modified:
  - src/app/mod.rs
  - src/app/serve.rs
  - src/main.rs
  - tests/cli_integration.rs
autonomous: true
requirements:
  - ROL-05
  - CORE-06
  - CORE-07
user_setup: []
must_haves:
  truths:
    - CLI-facing `serve` orchestration moves behind a dedicated app-service seam.
    - Real-fixture contract tests prove migrated `skills` and `serve` flows still behave correctly.
  artifacts:
    - new `src/app/serve.rs` request/service layer exists
    - CLI integration coverage explicitly pins migrated `skills` and `serve` behavior
  key_links:
    - Phase 12 can rely on explicit contract gates instead of assumption when reviewing the migrated route
---

<objective>
Extract the top-level `serve` wrapper and add explicit contract gates for the
migrated command families.

Purpose: `serve` still mixes registration setup and transport dispatch inline
in `main.rs`, and the migration proof for `skills`/`serve` needs to be
explicit before soak review.
Output: dedicated `app::serve` orchestration plus real-fixture contract tests
for migrated `skills` and `serve` flows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-command-family-extraction-contract-gates/11-CONTEXT.md
@.planning/phases/11-command-family-extraction-contract-gates/11-RESEARCH.md
@.planning/phases/11-command-family-extraction-contract-gates/11-VALIDATION.md
@src/main.rs
@src/app/mod.rs
@src/app/watch.rs
@tests/cli_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move top-level `serve` orchestration behind `app::serve`</name>
  <files>src/app/mod.rs, src/app/serve.rs, src/main.rs</files>
  <read_first>src/main.rs, src/app/watch.rs, src/server/mod.rs, .planning/research/ARCHITECTURE.md</read_first>
  <action>Create `app::serve` for registration prep, auth parsing, and transport dispatch while keeping `server::serve_stdio/http` as the underlying library boundary. Thin the `Commands::Serve` branch in `main.rs` to request assembly and delegation.</action>
  <verify>rg -n "ServeService|Commands::Serve|serve_stdio|serve_http" src/app/serve.rs src/main.rs</verify>
  <acceptance_criteria>
    - the top-level `serve` branch in `main.rs` is thin
    - `app::serve` owns CLI wrapper orchestration without duplicating server internals
    - existing serve behavior remains stable
  </acceptance_criteria>
  <done>`serve` has a dedicated CLI-facing app-service seam.</done>
</task>

<task type="auto">
  <name>Task 2: Add real-fixture contract gates for migrated `skills` and `serve` flows</name>
  <files>tests/cli_integration.rs</files>
  <read_first>tests/cli_integration.rs, .planning/ROADMAP.md, .planning/VALIDATION.md</read_first>
  <action>Add or tighten CLI integration tests that pin the migrated `skills` and `serve` families: `skills` list/info/install/update contract behavior, stdio hybrid serve exposure, and the existing migrated serve/watch reload flow or equivalent real-fixture regression.</action>
  <verify>cargo test --quiet cli_integration</verify>
  <acceptance_criteria>
    - migrated `skills` flows have explicit contract coverage
    - migrated `serve` flows have explicit contract coverage
    - the contract suite uses real fixtures or real temp installs rather than synthetic mocks only
  </acceptance_criteria>
  <done>The extraction is backed by explicit real-fixture contract gates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] focused CLI integration tests for migrated `skills` flows
- [ ] focused CLI integration tests for migrated `serve` flows
- [ ] existing serve-watch reload regression still passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `serve` command-family orchestration moves behind `app::serve`
- real-fixture contract gates cover the migrated `skills` and `serve` flows
  </success_criteria>

<output>
After completion, create `.planning/phases/11-command-family-extraction-contract-gates/11-02-SUMMARY.md`
</output>

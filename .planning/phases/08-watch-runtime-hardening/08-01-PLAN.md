---
phase: 08-watch-runtime-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/mod.rs
  - src/app/watch.rs
  - src/main.rs
autonomous: true
requirements:
  - CORE-05
user_setup: []
must_haves:
  truths:
    - `sxmc watch` runs through a dedicated app/runtime seam rather than living directly in the top-level command branch.
    - CLI request assembly, output rendering, and exit behavior remain compatible with the shipped command surface.
  artifacts:
    - `src/app/watch.rs` defines the watch request and service boundary.
    - `src/main.rs` dispatch for `Commands::Watch` becomes thin request assembly and service invocation.
  key_links:
    - Plan 08-02 can harden notification timing without still being coupled to a monolithic `main.rs` watch branch.
---

<objective>
Introduce a dedicated app/runtime seam for `sxmc watch`.

Purpose: the current watch loop lives directly in `src/main.rs`, mixing CLI
dispatch with long-running runtime behavior. Phase 8 needs a stable extraction
point before timeout and isolation work can land cleanly.
Output: a typed `WatchRequest`, `WatchService`, and thin `main.rs` dispatch
that preserve current watch behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-watch-runtime-hardening/08-CONTEXT.md
@.planning/phases/08-watch-runtime-hardening/08-RESEARCH.md
@.planning/phases/08-watch-runtime-hardening/08-VALIDATION.md
@src/app/mod.rs
@src/app/status.rs
@src/main.rs
@tests/cli_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the watch request and service seam</name>
  <files>src/app/mod.rs, src/app/watch.rs, src/main.rs</files>
  <read_first>src/app/mod.rs, src/app/status.rs, src/main.rs, .planning/research/ARCHITECTURE.md</read_first>
  <action>Create `src/app/watch.rs` with a typed `WatchRequest` and `WatchService` that own the watch loop orchestration. Update `src/app/mod.rs` to export the module. Refactor the `Commands::Watch` branch in `src/main.rs` so it resolves install paths, builds the request, invokes the service, and only handles the resulting exit code. Preserve existing flags, output format behavior, and exit semantics.</action>
  <verify>rg -n "struct WatchRequest|struct WatchService|Commands::Watch" src/app/watch.rs src/main.rs</verify>
  <acceptance_criteria>
    - `src/app/watch.rs` contains `WatchRequest`
    - `src/app/watch.rs` contains `WatchService`
    - `src/main.rs` no longer contains the full watch loop body
  </acceptance_criteria>
  <done>`sxmc watch` now runs through a dedicated app/runtime seam with thin top-level dispatch.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet test_watch_flushes_first_frame_for_piped_stdout`
- [ ] `cargo test --quiet test_watch_exit_on_unhealthy_fails_for_unhealthy_bakes`
- [ ] `rg -n "WatchService|WatchRequest" src/app/watch.rs src/main.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `watch` orchestration is no longer concentrated in the `Commands::Watch` branch
- The phase leaves notification hardening work with a stable seam instead of a monolithic branch
  </success_criteria>

<output>
After completion, create `.planning/phases/08-watch-runtime-hardening/08-01-SUMMARY.md`
</output>
